<import src="../template/template" />
<template is="title" data='{{title:"地图"}}' />

<view class='container'>
  <!-- audio -->
  <view class='item'>
    <text>map，小程序内置的地图比较简单，没办法根据地图动态添加marker，需要要知道经纬度。callout没实现。</text>
    <map id='my_map'
          longitude='{{longitude}}'
          latitude='{{latitude}}'
          markers='{{markers}}'
          circles='{{circles}}'
          show-location
          bindpoitap='bindpoitap'
    >
      <block wx:for='{{flags}}' wx:key='*this'>
        <cover-image id='{{item.id}}' src='/resources/images/cat.jpg' style='width:80rpx;height:80rpx;position:relative;left:{{item.left}}rpx;top:{{item.top}}rpx;' bindtap='flagTap'>
        </cover-image>
      </block>
    </map>
  </view>
</view>
